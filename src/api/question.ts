// import api from './index';
// import { CreateQuestionData } from '../types/question.types';

// export const getQuestions = () => {
//   return api.get('/questions');
// };

// export const getQuestion = (id: string) => {
//   return api.get(`/questions/${id}`);
// };

// export const createQuestion = (questionData: CreateQuestionData) => {
//   return api.post('/questions', questionData);
// };

// export const updateQuestion = (id: string, questionData: Partial<CreateQuestionData>) => {
//   return api.put(`/questions/${id}`, questionData);
// };

// export const deleteQuestion = (id: string) => {
//   return api.delete(`/questions/${id}`);
// };