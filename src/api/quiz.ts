// import api from './index';
// import { QuizAnswer } from '../types/quiz.types';

// export const getRandomQuiz = (count: number = 10) => {
//   return api.get(`/quiz/random?count=${count}`);
// };

// export const submitQuiz = (answers: QuizAnswer[]) => {
//   return api.post('/quiz/submit', { answers });
// };